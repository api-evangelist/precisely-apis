{
  "opencollection": "1.0.0",
  "info": {
    "name": "Precisely APIs Validatemailingaddress API",
    "version": "18.1.0"
  },
  "request": {
    "auth": {
      "type": "oauth2",
      "flow": "implicit",
      "authorizationUrl": "https://api.precisely.com/oauth/token",
      "credentials": {
        "clientId": "{{clientId}}"
      }
    }
  },
  "items": [
    {
      "info": {
        "name": "Validatemailingaddress",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "ValidateMailingAddress",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://api.precisely.com/addressverification/v1/validatemailingaddress/results.json",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "oauth2",
              "flow": "implicit",
              "authorizationUrl": "https://api.precisely.com/oauth/token",
              "credentials": {
                "clientId": "{{clientId}}"
              }
            }
          },
          "docs": "ValidateMailingAddress analyses and compares the input addresses against the known address databases around the world to output a standardized detail."
        }
      ]
    }
  ],
  "bundled": true
}